This javascript folder contains most of the function definitions for controlling data.
The angular folder contians one file app.js that displays the file in one line.
The styles folder contains files that change the how the pages are styled.
